# [Homelab Name] — Lovelace Dashboard Index

**Source of truth:** `ha-dashboards/*.yaml` in this repo — deploy via `./deploy-ha-dashboards.sh`
**Managed dashboards:** <!-- count -->

> <!-- Fill in: note whether dashboards are in YAML mode (UI editor disabled) so editors know to edit YAML, not the UI. -->

---

## Managed Dashboards

| File | HA Title | URL Path | Sidebar | Admin Only | Icon |
| :--- | :--- | :--- | :--- | :--- | :--- |
| <!-- Fill in: one row per managed dashboard --> | | | Yes/No | Yes/No | `mdi:...` |

**Not managed (excluded):**
<!-- Fill in: dashboards intentionally left out of git (auto-generated default, built-in map, etc.). -->

---

## Deploy Workflow
<!-- Fill in: the edit → diff → deploy → refresh → commit steps, and what the deploy script does under the hood. -->

---

## Adding a New Dashboard
<!-- Fill in: the extra steps a brand-new dashboard needs (registering it in configuration.yaml, restart, index update). -->

---

## Rollback
<!-- Fill in: how to revert a dashboard change (git revert + redeploy) and the full-restore path. -->
